{
  "name": "@chromawave/brand-assets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generates the Chroma Wave production asset system from the identity document geometry",
  "scripts": {
    "build": "node src/generate.mjs",
    "typecheck": "node --check src/generate.mjs",
    "lint": "node --check src/generate.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "sharp": "^0.34.4"
  },
  "devDependencies": {
    "@expo-google-fonts/space-grotesk": "^0.4.1",
    "@types/opentype.js": "^1.3.10",
    "opentype.js": "^2.0.0"
  }
}
