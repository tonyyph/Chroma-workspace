# Cloud integration is intentionally disabled in the first local-first slice.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=
REVENUECAT_IOS_API_KEY=
REVENUECAT_ANDROID_API_KEY=
SENTRY_DSN=

# Field notes — the weekly editorial drop.
# A static JSON array of TrendingItem on any CDN; no service required.
# Unset, the app ships with its bundled catalogue and never reaches the network.
EXPO_PUBLIC_FIELD_NOTES_URL=
